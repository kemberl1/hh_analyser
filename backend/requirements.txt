# Web framework
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
gunicorn>=23.0.0,<24.0

# Database
sqlalchemy[asyncio]>=2.0.40,<3.0
psycopg[binary,pool]>=3.2.0,<4.0
alembic>=1.15.0,<2.0

# Validation & config
pydantic>=2.11.0,<3.0
pydantic-settings>=2.9.0,<3.0

# HTTP client (Phase 2+)
httpx>=0.28.0,<1.0

# Scheduler
apscheduler>=3.11.0,<4.0

# Logging
structlog>=25.0.0,<26.0

# Testing
pytest>=8.0.0,<9.0
pytest-asyncio>=0.25.0,<1.0

# Linting
ruff>=0.11.0,<1.0
black>=25.0.0,<26.0

# Numeric (Phase 4+)
numpy>=2.0.0,<3.0

# LLM SDK (Phase 6+)
openai>=1.80.0,<2.0

# Retry / rate-limit (Phase 2+)
tenacity>=9.0.0,<10.0
aiolimiter>=1.2.0,<2.0

# HTML parsing (Phase 2+)
selectolax>=0.3.0,<1.0
beautifulsoup4>=4.13.0,<5.0

# PDF/DOCX parsing (Phase 7 — resume analyser)
pdfplumber>=0.11.0,<1.0
python-docx>=1.1.0,<2.0

# Multipart form data (Phase 7 — file upload)
python-multipart>=0.0.20
